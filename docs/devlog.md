# 研究日志

按时间倒序记录每次研究会话的工作内容。

---

## 2026-04-15 08:08 — 项目目录整理 + P1 6 人传记修订（全链路校验 + 质量补救 + 路径教训沉淀）

> 会话 `27c943ac-9dd5-4837-9298-757634127511` | 时长约 14h 39m

### 摘要
上午做项目根目录整理并 commit/push（ce93383）；下午到深夜用 Kimi+Codex 对 P1 高风险 6 人做传记重建，期间踩了两个大坑（Python 后台 2 分钟静默死亡、Codex heredoc/stdin 卡死），摸索出"Agent 前台直调 CLI"稳定路径后跑完 3 阶段校验+BIO-005/006 全套，用户质疑"质量是否真提升"触发两轮新旧对比评审，发现 Kimi 整篇重写导致一手来源 -17.6%，做了合并补救+定点精修才真正闭环。本次最大收获是**"已有传记不该用 Kimi 整篇重写"的方法论教训已落入 CLAUDE.md 决策树和记忆**。

### 讨论过程

**第一阶段：项目目录整理（08:08–11:00 前后）**
- 用户要求"整理一下目录"，识别根目录散乱：三个并行的人物目录（人物名录/人物传记/人物档案）、6 个 batch_biography 版本、多个临时文件（nul、audit_tmp.json、bio_*.py 等）、两个过时备份目录
- 派 Explore 调研确认：人物名录是唯一活跃目录、两个备份主文件无后续改动、v1-v5 无现役引用、v6 是当前最新
- 用户确认方案：人物目录合并为一、脚本只留最新归入 tools/、备份可清
- 执行代理合并/清理/迁移时**误删 v6**（未跟踪文件被代理一并 rm 掉无法恢复）
- 用户要求把"乱放文件"问题落到规则层，写入 CLAUDE.md「目录与文件管理规则」6 条 + 第 7 条"批量删除前白名单交叉校验"（v6 事故的防再犯规则）
- commit `ce93383` + push，完成整理

**第二阶段：P1 清单与路径选择（12:00 前后）**
- 用户要"跑遗留的 P1 高风险"，代理调研定位 13 人清单
- 讨论串行 vs 并行，用户定"2 个 2 个"
- 起初用 `tools/batch_biography.py`（由代理基于 v3 重写）+ `run_in_background=true` 启动

**第三阶段：两次后台 Python 静默死亡（13:07 / 15:23）**
- 第一次：脚本启动后 2 分钟死亡，日志只有"[1/13] 张居正"，`tasklist` 无 python 进程、张居正.md 未被覆盖
- 诊断假设：subprocess.run capture_output 缓冲死锁 + Tee 编码冲突 → 改为 Popen 流式读 + 心跳文件 + UTF-8 强制
- 第二次：鲁棒版同样在**启动 2 分钟时心跳停止**（比第一次多走一步但仍死）
- 深度诊断：不是 Python 的问题，是 Claude Code 的 `run_in_background` 对长时间无 stdout 的后台进程有隐性回收，Python 中间层放大了症状
- 写入 `docs/biography_validation_pipeline_design.md` 第 16 节（问题 D 诊断 + T1-T4 验证方案）

**第四阶段：转向 Agent 前台直调 CLI（15:45 后）**
- 用户提示"之前调用都没问题，问题出在 Python 脚本之后" → 方向定为去掉 Python 中间层
- Agent 在前台 Bash 里直接调 `kimi --print -p "<prompt>"`，每 Agent 跑 1 人，每批 2 人并发
- 张居正 7 分钟归档、海瑞 12.5 分钟归档，模式验证
- 继续朱翊钧/王喜姐、郑贵妃/李时珍 归档（耗时 8-33 分钟不等）
- 李太后/麻贵 Agent 收到 `429 rate_limit_reached_error`，Kimi 日配额耗尽（共用 7 次），剩余 7 人顺延

**第五阶段：Codex 校验路径的三次卡死与解法（16:00–17:15）**
- 用户决策"先完成 Codex 校验"
- 首次派 2 个校验 Agent（张居正 + 海瑞），用 heredoc 嵌入多段 prompt + bash 反斜杠续行
- 60 分钟无产出，3 个 codex.exe 进程活着但文件未写入
- Agent 自诊断日志命中：**heredoc 展开后的 stdin 未关闭让 Codex 一直等 stdin 输入**
- 主窗口做最简 Codex 测试（单行 "请说你好"）8 秒返回，证实问题在 prompt 结构
- 规避方案确定：**单行位置参数 + 不用反斜杠续行 + `< /dev/null` 显式关 stdin + timeout 900 硬超时**
- TaskStop 2 个卡死 Agent + taskkill codex.exe 进程

**第六阶段：Codex 三阶段批量化（17:15–18:30）**
- 阶段 A（综合校验，read-only）：6 人共 95 条工单（高 38/中 47/低 10）
- 阶段 B（按工单修订，workspace-write）：处理 39 条高优、跳过 56 条中/低（末尾加"## 修订记录"小节）
- 阶段 C（联网核对，read-only + WebFetch）：60 条核对，一致 38/偏差 21/不可达 1
- 调度节奏：每阶段 2 人一批，每 Codex 调用平均 2-7 分钟
- 全程 18 次 Codex 调用全部成功

**第七阶段：BIO-005 + BIO-006 收尾（18:30–19:00）**
- BIO-005 跨文件一致性：8 条冲突（严重 5/中等 2/轻微 1），典型如王恭妃受幸时点、清算张居正起点、郑氏入宫时点
- BIO-006 审计脚本：R1-R6 全部 36/36 通过（脚本实际路径在 `_validation/tools/bio006_audit.py`，代理 prompt 里给错了路径但代理自己找到）
- 综合汇总报告 196 行

**第八阶段：用户质疑"是否真提升"触发 v1 评审（19:30–21:00）**
- 用户问"重写后质量相比之前是否提升？如何判断？"——关键方向问题
- 坦诚：我只做了 BIO-006 合规性扫描，没做真正的新旧对比
- 派 Codex v1 评审：读 git HEAD 旧版 + 当前新版 逐人评审
- 震撼结果：**2/6 显著提升（王喜姐/李时珍）、3/6 退化（张/朱/郑）、1/6 轻微退化（海瑞）**
- 一手来源均值 -17.6%、张居正 5 条 04-12 修复被 Kimi 重写回滚、朱翊钧引入知乎/腾讯新闻/网易号、郑贵妃"大高玄殿盟誓"从存疑区爬回正文
- 核心洞察：**成功都是"定点修订"，失败都是"Kimi 整篇重写"** —— Kimi 重写对已有传记是反向操作

**第九阶段：合并补救（21:00–22:00）**
- 用户追加指令"新文档有遗漏的旧文档内容，补充进去"
- 派合并 Agent 对 6 人做 Codex 判定"应补/应舍"后精准合并
- 结果：25 条回补 / 33 条正确舍弃，+21 卷次、+89 行
- 张居正 36→73（超过旧+今早并集）、朱翊钧 32→46、海瑞 56→39（仍低于旧版，因旧版《海南日报年谱》月日条目按规则被全舍）
- 王喜姐第一次 Codex 误判方向（因旧版里有 04-12 修订记录小节误以为旧版是新的），重试成功——发现"修订记录小节是跨版本比较的信号污染源"

**第十阶段：v2 评审 + 定点精修 + 规范沉淀（22:00–22:45）**
- v2 评审：**5 提升 / 1 退化**（张/海/朱/郑从退化翻转为提升，李保持提升，王喜姐逆转为退化）
- 王喜姐问题：合并时 Codex 判舍过严（一手 15→7），正文残留"同居二十余年"新错
- 海瑞问题：一条鞭法角色争议 v1 部分修复 / v2 未修复
- 派 2 个定点精修 Agent：王喜姐回补 2 + 修正 4（一手 7→30，"同居二十余年"按《酌中志·卷十七》订正为"约十六年"）；海瑞订正 4 处（一条鞭法节制为应天十府试行、族属改"尚无定论"）
- 同时自主写入 2 份新记忆 + CLAUDE.md 多 Agent 规范新增"工作流选择决策树"子小节（第 175-207 行）

### 关键决策

- **放弃 Python 后台脚本跑 Kimi/Codex**：两次静默死亡后断言 `run_in_background + Python 中间层`在 Claude Code Windows 下不可用，立即切到 Agent 前台阻塞直调 CLI 的模式
- **Codex 调用的强制约束**：单行位置参数 + 无反斜杠续行 + `< /dev/null` + timeout 硬超时——heredoc 卡死事故后沉淀为所有 Codex 调用的标准规范
- **引入新旧质量评审作为 commit 前拦截**：不再只信 BIO-006 合规性扫描，任何修订 commit 前需验证"一手来源数不下降超过 15%"
- **"已有传记走工作流 C 不走 B"**：P1 批次是对该规则的反面教材，教训写入 CLAUDE.md 决策树 + 记忆 `feedback_avoid_full_rewrite.md`
- **自主化升级**：用户两次明确放权（"细节不用确认"→"过程迭代自启自记"），我建立 `feedback_autonomy_level.md` 并在 CLAUDE.md 工作原则新增第 6 条边界
- **合并补救后再跑 v2 评审验证**：不偷懒接受合并结果，Codex 评审 v2 证实 5/6 提升才敢说闭环

### 否决/放弃的方案

- **后台跑 `tools/batch_biography.py`**：两次失败后彻底放弃
- **后台跑 `tools/validate_biographies.py`**：同根问题，启动后就让它空转然后取消
- **单 Agent 串联 Codex 三阶段**：heredoc 卡死后改为主窗口调度多 Agent，每 Agent 只跑一个阶段
- **opencode+glm-5.1 备用生成器跑剩余 7 人**：CLAUDE.md 警告其质量风险，明日 Kimi 配额刷新再跑
- **直接接受 v1 评审的"6 人修订合规"表面结论**：用户追问"真提升吗"倒逼出完整的评审+合并+v2 闭环，这个质疑至关重要

### 未完成

- **未 commit**：本次大量改动（6 份传记 + 校验产物目录 + CLAUDE.md 决策树 + docs 第 16 节 + tools 鲁棒版脚本 + 2 份记忆）尚未 commit，等用户授权
- **剩余 7 人顺延**：李太后/麻贵/谭纶/李成梁/努尔哈赤/陈矩/顾宪成，Kimi 配额刷新后续跑。按本次教训**必须走工作流 C 定点修订，不要 Kimi 重写**
- **BIO-005 严重冲突 5 条未用户裁决**：清算张居正起点、王恭妃受幸时点、郑氏入宫时点、朱常洵出生日、神宗与王后关系
- **阶段 C 偏差 21 条未二次修订**：郑贵妃 6 条、王喜姐 4 条、朱翊钧 3 条、李时珍 4+1 条、张居正 2 条、海瑞 2 条
- **问题 D 的 T1-T4 对照实验未做**：零 Kimi 成本验证 run_in_background 死因，推迟
- **海瑞卷次 39 仍低于旧版 56**：合并时正确舍弃《海南日报年谱》低学术源，Codex v2 仍判"显著提升"因一手来源 10→23 涨幅更大，但这个数据差异在后续应有新闻报道/真正年谱核对补强
- **遗留未 commit 项**：`.claude/scheduled_tasks.lock`、之前的 `docs/devlog.md` 旧条目 + 多份 04-14 工作区 modified 文件（本轮不混入）

---

## 2026-04-14 08:03 — 遗留任务收尾：04-12 分段 commit + 03-29 的 P1/P4 研究补齐

> 会话 `681fec2e-eea2-4536-a7d3-d3c67c97b1d6` | 时长约 15h 43m（跨两日，大部分为 Kimi 后台执行等待时间）

### 摘要
从"查看 devlog 继续未完成任务"开始，两条主线：① 把 04-12 会话遗留的大量未提交改动拆成 5 段 commit 入库；② 补齐 03-29 遗留的 P1（市镇非棉业）和 P4（乡村微观数据）两个研究课题，走完 Kimi → Codex → Opus 标准流水线。期间发现 900s Kimi 超时问题复现，改到 1800s 后通过。

### 讨论过程

**第一阶段：04-12 遗留入库（16:20 前后）**
- 打开 devlog 看到 04-12 有大量未提交改动（CLAUDE.md 修改 + 67 份传记 + 校验工具 + 45 份角色视角 + 流水线脚本），用户确认"可以分段 commit"
- 盘点工作区：tracked 3 个修改，untracked 67+ 个条目。按内容拆成 5 段：
  - ① 流水线基础设施（9 个 .py + logs/ + .claude/）→ `bb2cd97`
  - ② 人物名录 67 篇 + INDEX + 12 个分类子目录 → `1f2455f`
  - ③ 校验体系（_validation + _backup_BIO001 + _backup_训练版 51 份）→ `d097e72`
  - ④ 角色视角 45 篇 + 专题研究 + 文献收集 + 主题目录新增 → `4c0a4cb`
  - ⑤ CLAUDE.md + devlog + sources 修改 → `fa3b362`
- `nul` 文件无法入索引（Windows 保留字），跳过

**第二阶段：核实 03-29 是否真的完成（~17:00）**
- 用户问"03-29那段应该都完成了吧？"触发核查
- 实际状态：git commit 已做完（本次 commit 流程覆盖），但 P1/P4 深度问题仍未解——南翔非棉业缺失、泗泾深度不足、农户收支账簿完全缺失
- Kimi 超时在 research_pipeline.py 里仍是 900s
- 用户纠正：Kimi 在 04-12 已跑通、Codex 质量最好应该用来验证 → 方向对齐到"Kimi 搜集 + Codex 校验"流水线

**第三阶段：P1/P4 研究（17:00 ~ 次日凌晨）**
- 起初两路 Kimi 并行（通过代理后台运行），都在 900 秒超时——复现了 03-29 的老问题
- 诊断后用户决定"改到更长再试"，将 Kimi/Codex 超时从 900 → 1800
- P1 成功：976 行，耗时 1687 秒（28 分钟）——恰好卡在新超时边界内
- P4 的 Kimi 也跑完了产出文件，但 pipeline Python 进程已经退出（代理超时），文件只在 `/e/System_Temp/kimi_research_c21p96mt/` 临时目录。手动搬到 `角色视角/` 下，337 行（低于 400-700 目标但可用）
- Codex 校验两份初稿：都成功。P1 发现 7 个问题（3 严重）；P4 发现 12 个问题（5 严重）+ 6 条一手史料补充建议
- 用户提议让 Codex 不仅审核还独立研究。试了——两份都只产出了 87 行的方法论框架，不满足质量门槛
- 诊断：Codex 在 `exec --sandbox read-only --ephemeral` 模式下，有输入文件锚定时表现好（校验），无输入文件时倾向产出框架而非内容（独立研究）
- 回退到方案 1：用 Kimi 初稿 + Codex 校验进入 Opus 合并
- Opus 合并两份终稿，均按证据分层标注（P1 三层：明代可证/清代追溯/区域推断；P4 四层：明代一手/明末清初过渡/清代材料/学者重建）。P1 终稿 318 行，P4 终稿 444 行

**第四阶段：清理与最终 commit**
- 按 CLAUDE.md 合并规范删除 6 份 `_kimi.md` / `_codex.md` / `_codex_review.md` 草稿 + 1 份测试残留
- 两段 commit：`908cc1a`（超时调优）+ `dd006b2`（P1+P4 终稿 762 行）

### 关键决策

- **分 5 段 commit 而非一次性大 commit**：04-12 改动跨越流水线基础设施、传记建档、校验工具、角色视角研究、规范更新五个相对独立的维度，按主题拆分便于未来回溯和可能的 revert
- **Kimi 超时 900 → 1800 改为全局配置**：用户在 03-29 遇到过同样超时但当时没根治，这次直接改源码。实测 P1 耗时 1687 秒，证实 1800 秒是合理边界
- **放弃让 Codex 独立研究**：实测两份产出都是"研究方案建议书"不是"研究报告"，不满足 CLAUDE.md 的草稿质量门槛（古籍引用须有卷次）。Codex 在 pipeline 的 `exec --sandbox` 模式下，独立研究能力远弱于校验能力
- **手动搬运 P4 而非重跑**：P4 的 Kimi 实际已完成写入临时目录，只是 pipeline 父进程提前退出没收成果。重跑会浪费 20+ 分钟，直接从 `/e/System_Temp/kimi_research_c21p96mt/` cp 过来即可
- **证据分层标注写入终稿**：P1 三层、P4 四层，不是简单的"可信/不可信"二分。这延续了 04-12 会话里 E1-E6 证据链规范的思路，让历史材料的时段差异可见

### 否决/放弃的方案

- **一次性大 commit**：被用户主动拆分成 5 段
- **Kimi 重跑 P4**：临时目录文件已存在，手动搬运即可
- **Codex 独立研究 P1/P4**：产出质量不达标，回退到"Codex 仅校验"
- **给 Codex 具体 URL 让它逐条 fetch**：与既有的 _codex_review 功能重叠，冗余

### 未完成

- **04-12 遗留的其他项未处理**：21 份高风险文件 Kimi 重跑、证据链规范 64 份未迁移、BIO-007/008/009 审计脚本实现、小西行长/利玛窦重写、devlog 手工化改造。用户没指示继续推进
- **未 push**：当前 master 领先 origin 8 个 commit
- **工作区残留**：`.claude/scheduled_tasks.lock`（工具状态文件，不该入库）+ `nul`（Windows 误操作产物，上次就有），均未清理
- **Kimi 超时根治方案不完善**：仅改到 1800 秒能过目前 prompt 规模，但更复杂的 prompt 仍可能超时。更根本的办法是让 Kimi 流式输出（不等完整 capture_output）或分阶段调用，未实施
- **本条 devlog 仍是 AI 风格**：用户在 04-12 提过希望 devlog 手工化，这条记录仍按 AI 风格写入

---

## 2026-04-12 08:28 — 人物名录建档 + 全库校验修订 + 工作流工具化

> 会话 `58bf2b93-1087-4f61-a0d0-78f54c99e2ea` | 时长约 33h 54m（跨 3 日）

### 摘要
从用户"收集人物信息"的一句话需求出发，完成了三日连续工作：67 人明朝人物传记建档 → 多轮 Codex 校验发现系统性质量问题 → 12 轮修订（单篇 + 全库规则）→ 工具化收尾（验收脚本集 + 证据链规范 + CLAUDE.md 更新）。期间走了多次弯路并做了根本性反思。

### 讨论过程

**第一阶段：建档（04-10 晚 ~ 04-11 中午）**
- 用户要求收集人物信息，先建名录再填充。确定 1550-1600 在世标准，12 类分类
- 主窗口启动 5 路 Claude 代理并行搜集 —— 用户立刻纠正："没有走 Kimi+Codex→Opus 流水线"
- 改用 Kimi 综合搜集，产出 `INDEX.md`（约 430 人）
- 用户选择先挑重点人物建档。第一批 17 人，Kimi 并发 17 路成功 10 人 + 接力重跑 5 人 + opencode 补 2 人
- 第二批起改用 opencode + glm-5.1，累计建了 62 份
- **关键纠正时刻**：用户注意到 opencode 轮次的文件普遍只有 100-200 行（vs Kimi 版 300-600 行），问"不是说要从网上搜的吗，怎么 prompt 里说基于训练知识？" —— 暴露出我把"避免 Explore Agent 卡死"过度推广成"禁用全部搜索工具"，导致后续 45 人都是 glm 凭训练知识编造
- 决定备份 51 份训练版到 `_backup_人物名录_training_only/`，然后用修正后的 prompt（允许 WebFetch，只禁 Agent）试跑 2 人验证，成功后批量重做
- 重做过程又遭遇连串问题：glm 默认模型 glm-4.6 有 SSE 超时、`question` 工具禁不掉导致死锁（备份文件夹在 `人物名录/` 内被模型读到，触发澄清问题）、模型固执地把文件写到 `人物档案/` `历史人物/` `05-军事与边防/` 等"更合理"的位置
- 解决：把备份移出 `人物名录/`、agent 文件显式指定 `model: zhipuai-coding-plan/glm-5.1`、在 Python 脚本里加 `find_and_move_file` 自动扫描搬运（放弃对抗模型路径固执）
- 最终 66 人齐活（小西行长因 3 次卡死只拿到 34 行 stub，后续重写到 257 行）

**第二阶段：校验（04-11 晚）**
- 用户提出"用 codex 对文档进行一下校验"
- 我最初建议 20 人独立校验。Codex 在张居正一篇校验中顺带看了 6 个文件，暴露效率：改为"一次调用校验 20 人"综合报告
- 综合报告 40 行精华，发现系统性问题：**争议说法定论化、同文自相矛盾、野史进入主叙述**。具体指出 6 条严重（张居正 1622 < 1630 时间倒置、海瑞族属+一条鞭法双重矛盾、李时珍蓬溪知县实为其子、王喜姐/郑贵妃野史进入正文、朱翊钧戏剧化细节）
- 用户追问："Codex 本身没法完成史料核实吗？" —— 我最初以为 Codex 只能找矛盾不能查史料，用户质疑后我重新检查，发现 Codex 第一轮报告里其实访问了 wikisource 做定点抽查。于是跑第三轮系统联网核对
- 第三轮 Codex 真的访问 wikisource/ctext/CBDB，对 13 个可疑点每条附带一手史料证据。例如《明史·李时珍传》卷299 原文"时珍官楚王府奉祠正，子建中，四川蓬谿知县"直接证伪"李时珍任蓬溪知县"说
- 用户确认"按照建议"：两路并行（A 修 6 条硬错误 + B Codex 联网核对）→ A2 修 4 条新硬错误（张居正西藏/麻贵回族/李成梁义子/陈矩逸闻）→ 全库工单 C → BIO-001~009 规则化
- 用户要求 5/6/7 并行：Phase 1 修剩余问题（BIO-005 跨文件/BIO-006 严重/5 份来源汇总）→ Phase 2 工具化/证据链/CLAUDE.md

**第三阶段：收尾（04-12 早）**
- BIO-005 跨文件一致性检查暴露单文件修订盲区：朱翊钧.md 已经降级"十岁太子"典故，但冯保.md 和高拱.md 还在写定论 —— 跨文件的同事件叙述需要专门的 BIO-005 环节，单文件修订不够
- BIO-006 六规则审计整体合规率 86.6%，发现 3 份严重不合规（小西行长截断、紫柏真可来源汇总截断、憨山德清 R2 搬运未到位）
- Phase 2 三路完成：工具化（`_validation/tools/` 5 个脚本 + 新传记验收流程.md）、证据链规范（E1-E6 六级 + BIO-007/008/009 三条新规则）、CLAUDE.md 更新（多 Agent 表格重构 + 工具能力边界 + 推荐工作流 + 六个教训）
- 收尾前用户指出 `docs/devlog.md` 现有内容"看起来不像本项目" —— 实际是 03-29 的府域设计研究记录，确实是本项目，但 AI 生成风格太重

### 关键决策

- **Kimi 走流水线，opencode 作备用**：Kimi 配额耗尽后需要备用生成器。确定 opencode + glm-5.1（显式指定模型）作为 Plan B，用 agent 配置文件禁用 read/task/list/glob 等工具
- **放弃对抗模型的路径固执**：与其在 prompt 里反复加"必须写到绝对路径 X"（无效），不如让 Python 脚本每次运行后扫描全项目找包含人物名的 md 文件自动搬运。这个决策抢救了约 20 个错位文件
- **接受 Codex 的"严重/中等/一般"分级**：Codex 自己用的分级格式比我设计的 ✅/⚠️/❌ 模板更好，直接采用
- **一次性综合校验 > 逐人独立校验**：Codex 天然擅长跨文件对比，20 人一次调用比 20 次独立调用高效得多
- **BIO-005 作为独立环节**：单文件修订会留跨文件矛盾（朱翊钧修了但冯保/高拱没同步）。必须在工作流里加一道"跨文件一致性检查"
- **证据链用 E1-E6 六级而非简单二分**：研究环境里内容有天然的层次差异，一刀切"可信/不可信"会误伤。六级允许渐进迁移、并存兼容
- **工具显式声明 model**：glm-4.6 SSE 超时教训后，agent 配置文件必须显式写 `model:` 字段

### 否决/放弃的方案

- **Claude 代理替代 Kimi 做搜集**：第一轮启动 5 路 Claude 代理，被用户立刻否决，改用 Kimi 流水线
- **opencode prompt 禁用全部搜索工具**：为了避免 Explore Agent 卡死而禁用所有 Glob/Read/ls/WebFetch/Agent，结果误伤 WebFetch 导致 45 人是训练知识版。正确做法是只禁 Agent，保留 WebFetch
- **在 prompt 里反复强调绝对路径**：模型固执不听，白费 token。改用脚本搬运
- **BIO-002 全库逐条引用打 6 级标签**：太庞杂，改为轻量版"识别高风险文件并加顶部警示块"
- **BIO-004 全库机械重写时间**：改为"只修硬错误（第 1 类年号换算错），其余列入待审"

### 未完成

- **21 份高风险文件未用 Kimi 重跑**：BIO-002 识别出百科依赖超 50% 的文件（外国人物 4 份百科占 80%+：丰臣秀吉、利玛窦、努尔哈赤、范礼安；宗教人物 2 份：莲池袾宏、憨山德清；文人学者多份）。等 Kimi 配额恢复后应重做
- **证据链规范只示范了 3 份文件**：张居正、海瑞、李时珍各改了关键事迹前 5 条。全库 67 份的完整迁移未做
- **BIO-007/008/009 三条新规则未合入主审计脚本**：规范文档已写完接入点说明，但 `_audit_script.py` 的新版本未实现
- **小西行长仍是 257 行重写版**：3 次 opencode 尝试中 2 次卡死；重写版基于训练知识 + 部分 WebFetch，没有完全从一手史料走一遍
- **利玛窦只有 111 行**：opencode 产物偏短但内容有实质，未重跑
- **`docs/devlog.md` 的手工化改造**：用户提出希望手工记录开发日志，现有日志（包括本条）都是 AI 风格。给了用户 3 个方案（归档旧+新建/替换/并存），用户未选就调用 `/wrap`，本条仍按 AI 风格写入
- **未 commit**：本次会话修改了 CLAUDE.md、sources.md，新增了 batch_biography_v2-v4_opencode.py、research_pipeline.py、人物名录/（67 份传记 + _validation/）、logs/、_backup_人物名录_training_only/、角色视角/（45 份）、专题研究/、文献收集/、docs/devlog.md 本条、batch_research.py 等。待用户决定 commit 策略（建议按阶段分多个 commit：① 流水线基础设施 ② 人物名录 67 份 ③ 校验报告和工具 ④ CLAUDE.md 规范更新）

---

## 2026-03-29 16:31 — 府域设计五课题批量研究

> 会话 `5e6f4eae-d219-49b4-9857-691079e917b6` | 时长约 1h 54m

### 摘要
基于游戏项目的府域设计需求文档（`prefecture-design-requirements.md`），完成了 5 个史料研究课题的全流程：从规范制定到多 Agent 搜集、Opus 合并、终稿写入。

### 讨论过程
- 用户问项目是否有预设研究流程，确认已有 4 步研究会话流程
- 用户追问是否有多 Agent 协作规范，确认没有
- 起草了多 Agent 协作研究规范（Agent 角色、草稿标准、质量门槛、冲突仲裁、合并规范、用户介入点），写入 CLAUDE.md
- 用户指定府域设计需求文档为本次研究目标，要求全权控制流程
- 读取需求文档，识别 P1-P5 五个课题，分析已有资料覆盖度
- 制定三批执行计划（新研究 → 整合补充 → 快速评估）
- 初始方案使用 Claude 子代理做 WebSearch——用户纠正：应使用 Kimi + Codex 流水线，不要用 Claude 替代
- 切换到 `research_pipeline.py` 调 Kimi + Codex，4 条流水线并行启动
- Kimi 4 个课题仅 1 个成功（松江水运），其余 3 个全部 900 秒超时；Codex 4/4 成功
- 对 Kimi 失败的课题，用 Claude 子代理的搜集结果作为替代输入
- 4 个 Opus 合并代理并行处理，全部成功
- P5 盐业快速评估发现"值得做节点"的意外结论（盐→棉产业转型链）
- 收尾：写入 P5 终稿、清理草稿、更新 sources.md

### 关键决策
- **制定多 Agent 协作规范**：写入 CLAUDE.md 成为项目约束。Kimi 做广度搜集、Codex 做深度校验、Opus 做裁决合并，冲突时"一手古籍 > 二手转述 > 百科"
- **研究必须走多模型流水线**：用户明确要求"他们有他们的特点，要尽量使用"。即使 Kimi 超时，也应优先尝试流水线，Claude 仅作备用
- **P5 盐业结论反转**：原以为松江盐业规模小不值得做节点，实际调研发现下沙盐场是浙西之冠，且盐→棉转型链有极高游戏设计价值
- **每份终稿增加"设计对接参考"章节**：虽然研究与游戏项目分离，但在终稿末尾提供可直接对接空间系统和经济系统的结构化数据

### 否决/放弃的方案
- **Claude 子代理替代 Kimi/Codex**：初始方案被用户否决，已切换到规范流水线
- **等待 Kimi 全部完成再合并**：Kimi 持续超时，改为 Kimi 失败时用 Claude 输出替代

### 未完成
- **Kimi 超时问题未根治**：900 秒超时对自定义 prompt 不够，需考虑提高到 1800 秒或拆分子任务
- **P1 市镇经济的非棉业特色**：朱家角米业、泗泾综合市场、南翔手工业等仍缺乏深度研究（Kimi 超时未跑出来），现有终稿主要基于已有棉业文档
- **P4 乡村经济微观数据**：农户精确收支账目、松江各县佃农比例等仍为估算值，缺少一手档案支撑
- **研究成果尚未提交 git**：5 份终稿 + CLAUDE.md 更新 + sources.md 更新均在工作区，未 commit
